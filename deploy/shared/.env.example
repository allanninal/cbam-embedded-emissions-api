# Copy to deploy/shared/.env on the server (never commit). Used by the SHARED Redis
# and APISIX gateway. Generate a strong value: openssl rand -hex 24
# This MUST be the SAME value every project's .env uses (cbam, c2pa, eudr).
REDIS_PASSWORD=change-me-to-a-long-random-secret
